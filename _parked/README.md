# Temporarily parked pages

These pages are removed from navigation and sitemap but kept for restore:

- `home-visit.html`
- `lab.html`
- `equipment.html`

## Restore

```bash
mv _parked/home-visit.html _parked/lab.html _parked/equipment.html ./
# then re-add nav links / sitemap entries (or ask the agent)
```
